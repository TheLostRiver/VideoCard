services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hardware_platform
      POSTGRES_USER: hardware
      POSTGRES_PASSWORD: hardware_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
